<UserControl x:Class="LOC.MVVM.Views.FooterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LOC.MVVM.Views"
             xmlns:controls="clr-namespace:TopUI.Controls;assembly=TopUI"
             xmlns:define="clr-namespace:LOC.Define"
             mc:Ignorable="d" 
             d:DesignWidth="800">
    <Border Background="Gainsboro">
        <controls:Footer MachineName="{Binding Source={x:Static define:MachineInfo.MachineName}}"
                         SoftwareVersion="{Binding Source={x:Static define:MachineInfo.SoftwareVersion}}"
                         VersionDescription="{Binding Source={x:Static define:MachineInfo.VersionDescription}}">
        </controls:Footer>
    </Border>

</UserControl>
