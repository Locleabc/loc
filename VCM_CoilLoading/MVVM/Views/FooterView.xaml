<UserControl x:Class="VCM_CoilLoading.MVVM.Views.FooterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VCM_CoilLoading.MVVM.Views"
             xmlns:controls="clr-namespace:TopUI.Controls;assembly=TopUI"
             xmlns:define="clr-namespace:VCM_CoilLoading.Define"
             mc:Ignorable="d" 
             d:DesignWidth="800">
    <Border Background="Gainsboro"
            CornerRadius="0 0 10 10">
        <controls:Footer MachineName="{Binding Source={x:Static define:MachineInfor.MachineName}}"
                         SoftwareVersion="{Binding Source={x:Static define:MachineInfor.SoftwareVersion}}"
                         VersionDescription="{Binding Source={x:Static define:MachineInfor.VersionDescription}}">
        </controls:Footer>
    </Border>
</UserControl>
